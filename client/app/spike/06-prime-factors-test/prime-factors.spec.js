import {describe, expect, it} from '../test-helper';

describe('prime factors of', () => {
  it('prove the test infrastructure works', () => {
    expect(true).to.equal(true);
  });

  it('the pipeline works', () => {
    expect(true).to.equal(true);
  });
});
